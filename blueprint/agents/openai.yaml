interface:
  display_name: "Blueprint"
  short_description: "Create visual plan for human review and code-agent execution"
  default_prompt: "Use $blueprint to create a visually rich plan.md file for this task that easily renders."

policy:
  allow_implicit_invocation: true
