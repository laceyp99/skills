interface:
  display_name: Prelude
  short_description: Investigate issues before choosing an action
  default_prompt: "Use $prelude to find the actual cause and recommend a path before we act."

policy:
  allow_implicit_invocation: false
