interface:
  display_name: "HTML Communication"
  short_description: "Create readable internal HTML docs"
  default_prompt: "Use $html-communication and create a clear, self-contained HTML artifact for this material."

policy:
  allow_implicit_invocation: true
