interface:
  display_name: "Grill Me"
  short_description: "Stress-test plans one question at a time"
  default_prompt: "Use $grill-me to pressure-test this plan or design through interactive decision making."

policy:
  allow_implicit_invocation: true
