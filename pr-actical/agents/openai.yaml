interface:
  display_name: "pr-actical"
  short_description: "Push a branch and create or update a GitHub draft PR."
  default_prompt: "Use $pr-actical to push this ready branch and create or update a GitHub draft PR safely."

policy:
  allow_implicit_invocation: true
