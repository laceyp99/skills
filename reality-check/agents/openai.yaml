interface:
  display_name: "Reality Check"
  short_description: "Find concrete code issues in branches and PRs"
  default_prompt: "Use $reality-check to inspect this diff and report prioritized correctness and maintainability findings."

policy:
  allow_implicit_invocation: true
